/** covfixer — the test-coverage configuration of the fixflow engine. */
export { type PlanEntry, analyze } from './analyze';
export { newCoverageEngine, newCoverageEngine as newEngine } from './coverage';
export { triage } from './triage';
